PKGFLAG   = -DHAVE_CBLAS=1
EXTHDRS   = 

include ../Makefile.in

BINDIR    = ../../bin
TARGET    = $(BINDIR)/$(EXEC)

target: $(TARGET)

$(TARGET): $(COBJS) $(LIBDEPEND)
	@echo "> building $(EXEC)..."
	@$(CXX) $(COBJS) $(CFLAGS) -I$(EXTINCDIR) -L$(LIBDIR) $(INCLIB) -o $@

