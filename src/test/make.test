PKGFLAG   = 
EXTHDRS   = 

include ../Makefile.in

BINDIR    = ../../tests/bin
TARGET    = $(BINDIR)/$(EXEC)

target: $(TARGET)

$(TARGET): $(COBJS) $(LIBDEPEND)
	@echo "> building $(EXEC)..."
	@$(CXX) $(COBJS) $(CFLAGS) -I$(EXTINCDIR) -L$(LIBDIR) $(INCLIB) -o $@

