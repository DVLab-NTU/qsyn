PKGFLAG   = 
ifneq (,$(findstring -DOSX, $(OSFLAG)))
    PKGFLAG += -llapack -lblas -DHAVE_CBLAS=1
else
    PKGFLAG += -llapack -lblas -lgfortran -DHAVE_CBLAS=1
endif
EXTHDRS   = 

include ../Makefile.in

BINDIR    = ../../tests/bin
TARGET    = $(BINDIR)/$(EXEC)

target: $(TARGET)

$(TARGET): $(COBJS) $(LIBDEPEND)
	@echo "> building $(EXEC)..."
	@$(CXX) $(COBJS) $(CFLAGS) -I$(EXTINCDIR) -L$(LIBDIR) $(INCLIB) -o $@

